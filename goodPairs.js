/**
 * @param {number[]} nums
 * @return {number}
 */
const numIdenticalPairs = (nums) => {
	let sortedPairs = nums.sort();
	let counter = 0;
	for (let i = 0; i <= nums.length - 1; i++) {
		nums.forEach(item => {
			if ()
		})
	} 
};

console.log(numIdenticalPairs([1,2,3,1,1,3]))

/*

Input: nums = [1,2,3,1,1,3]
Output: 4

*/

// ------ FAILED ------ 